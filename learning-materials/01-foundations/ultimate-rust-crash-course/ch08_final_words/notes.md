# Section 8 — Final Words

## Concepts

- Single wrap-up lecture, no new material and no exercise.
- Suggested next steps from the instructor: finish the real Invaders project from the course repo (`https://github.com/CleanCut/ultimate_rust_crash_course`) with `crossterm`/`rodio`, then move on to "Ultimate Rust 2: Intermediate Concepts" for deeper generics, lifetimes, smart pointers, and concurrency.

## Must-know syntax

None — this is a reflection point, not new material.

## Exercise

None. If you want a closing checkpoint, re-run every `solution.rs` from `ch02` through `ch07` (via `rustc --edition 2021 <file> && ./<binary>`) and confirm you can explain each line without looking at `notes.md`.
