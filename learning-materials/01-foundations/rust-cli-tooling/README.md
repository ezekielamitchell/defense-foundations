# Rust CLI Tooling

**Sources:** Cargo, [`clap`](https://docs.rs/clap/latest/clap/), and built-in Rust testing  
**Activation:** P0, W30–W31

## Focus

Cargo project structure, `clap` v4 argument parsing, `Result`-based error propagation, modules, file I/O, and `cargo test`.

## Evidence

`wc-rs` v0 with clear help, non-zero failure behavior, tests, example output, and comparison notes against the Python solution.
