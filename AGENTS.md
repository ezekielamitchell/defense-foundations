# defense-foundations agent contract

This repository is the educational implementation surface for Aegis Nexus. It
contains curriculum architecture, learning-source routing, and the code used to
earn evidence. It is not the authority for dates, daily scheduling, company
status, or curriculum promotion.

## Authority and precedence

1. Explicit current user direction.
2. `/Users/house/aegis nexus/01_Daily/_Phase Config.md` and its hash-verified
   active reset manifest for Phase 0 dates, capacity, object identities, and
   current-period counters.
3. The Aegis curriculum MOC and phase map for the P0–P12 sequence and gates.
4. This repository's curriculum and learning-material projections.
5. Historical prose and cached/generated views.

Generated `AEGIS:PHASE0_PROJECTION` regions and
`docs/aegis-phase0-projection.json` are publisher-owned. Never hand-edit them or
activate a staged reset by changing a repository projection. A staged manifest
is inert until Aegis activates it and the projection publisher completes.

## Current implementation truth

- `projects/file_stats` is the Python half of Project 0. Inspect and test the
  current files before describing behavior.
- `projects/hello-stats` is the Rust half. Inspect and test the current files
  before describing behavior.
- Existing paths, Git history, course work, and historical evidence remain
  factual, but they do not receive duplicate current-period credit.
- A scheduled block, checked task, generated page, passing command with zero
  collected tests, or green dashboard state is not implementation evidence.

Preserve all pre-existing dirty work. In particular, do not restore, replace,
or reinterpret the five deleted Invaders audio files. Do not commit or push
unless the user explicitly requests it.

## Curriculum contract

- P0 is the only active phase. P1–P12 remain `not started` and undated until an
  evidence-backed promotion is explicitly authorized.
- The 18–24 month route in `curriculum/COMPETENCY_PATHWAY.md` is a planning
  envelope, not a deadline, guarantee, or automatic progression schedule.
- Project 0 uses a weekly `pair_cycle_id`: Python owns one file-statistics
  contract and Rust independently proves parity on identical fixtures. The two
  blocks may occur on different days when degree-critical commitments require
  it. Resume the oldest unmet criterion in each language; weekly parity never
  waives per-language proof.
- Paid resources are issue-bound references. Official documentation is primary;
  watching or finishing a course is not a phase gate.
- Three flagships organize later work. The individual project list is a menu of
  supporting artifacts, not 23 mandatory parallel builds.
- Degree-critical work and protected capacity displace optional agents,
  samplers, and support work before gate-owned foundation work is overloaded.

## Evidence and safety boundaries

Foundation evidence must identify the artifact, exact working directory,
command, exit code, observed output, changed paths, verdict, blocker, and next
command. Use `phase0-proof.v1` receipts where applicable. Agent overlays,
papers, endr work, scheduling, dashboards, and reset administration never earn
Foundation credit.

endr is an independently governed company lane. This repository may build
general educational competence relevant to its research- and simulation-first
path, but it does not change endr gates, authorization, ownership, or product
maturity. No curriculum activity grants weapons, targeting, live-vehicle,
classified/CUI, consequential-action, or operational-deployment authority.

## Safe checks

From the repository root:

```sh
python3 tools/validate_phase0_integrity.py
node --check mobile/app.js
node --check mobile/sw.js
git diff --check
```

Do not run the projection exporter merely to make a staged reset appear active.

## Complete-beginner curriculum amendment — 2026-09-19

The user authorized a full route refresh for complete-zero learning before a
separate reset. `curriculum/START_HERE.md`, `COMPETENCY_PATHWAY.md`,
`RESOURCE_SPINE.md` and `route-v2.json` define the prepared teaching route.
P0–P12 are stable IDs, not a mandatory numerical sequence. Main dependencies are
P0 → P1 → P2 → P4 → P6 → P8 → P10 → P11 → P12; P3/P5/P7/P9 are optional depth.
This supersedes the old 18–24-month teaching envelope and perception-first
flagship dependency. Assume no prior fluency while preserving historical facts.

The planned reset introduces Python first, then Rust and parity, with beginner
instruction before testing/CLI reference books. Existing sealed weekly studios,
source caps, dates/counters and generated projections remain in force until that
separate reset reconciles them. Never silently use prepared teaching policy to
change current reservations. No later phase is promoted. See
`curriculum/RESET_HANDOFF.md` for pending decisions and exact preservation rules.

## Active parallel curriculum amendment — September 19

Latest explicit user direction supersedes Python-first waiting and single-source
rules in the earlier preparation. Python, Rust, agents, battlefield AI and warfare
introductory tracks run in parallel, with one primary course per track. Follow
`curriculum/PARALLEL_COURSE_MAP.md` and `parallel-policy-v1.json`. Courses/projects
are 2–3 hours and curriculum readings one hour. Rust does not wait for Python
completion; parity still requires independent basic file handling in both.
P0 remains the only active phase; agent and research study earns no Foundation
credit. Current timing and capacity come from the active Aegis manifest. The
retained beginner-policy-v1.json is historical preparation, not current policy.
